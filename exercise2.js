

/*function Max (num1, num2) {
	if (num1 > num2); {
		return (num1);
	} else {
		return (num2); 
	}
	console.log ( Max(15,25));
}

	function MaxOfThree (num1, num2, num3) {
		if (num1 >= num2 || num3 && num2 <= num3) {
			return (num1);
		} else {
			return (num2);
		}	
	}
		console.log ( MaxOfThree(2,5,1));


function isVowel (char) {
	if (char = "a"|| "e" || "i" || "o" || "u" || "y") {
		return "true";
	} else {
		return "false";
	}
}
	console.log ( isVowel("a"));

function rovarspraket (newWord) {
	if (newWord != "a"|| "e" || "i" || "o" || "u") {
		return newWord.split(+ "o");
	}
}
	console.log (rovarspraket("today is fun"));   Cannot figure this one out

	function reverse(word) {
	return word.split("").reverse().join("");
}
	console.log (reverse("Karen"));*/






